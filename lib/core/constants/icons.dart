import 'package:flutter/material.dart';

class AppIcon {
  static const Icon iconHome = Icon(Icons.home);
  static const Icon iconHistory = Icon(Icons.history);
  static const Icon iconWallet = Icon(Icons.wallet);
  static const Icon iconSettings = Icon(Icons.settings);
  static const Icon iconNotifications = Icon(Icons.notifications);
}
